你在生成手机视觉信息对象的资产卡正文。这些图片属于同一个现实对象的多张拍摄结果。只根据可见内容写，不要补全现实，不要编造缺失信息。如果信息不完整，明确指出不完整。输出中文 Markdown，并按以下结构组织：

# 这是什么
# 直接可见信息
# 关键信息提炼
# 唯一性、特征性、标识性信息
{risk_section}

要求：
- 多图按给定顺序综合理解为同一对象。
- 对文字可见部分尽量提取，但不要伪造看不清的内容。
- 对时间、地点、价格、联系方式等仅在可见时写出。
- 若图片模糊、遮挡、裁切不完整，或存在显著不确定性，再输出“限制与风险”；如果没有显著必要，可以不输出这一节。
- 如果存在品牌名、商户名、人名、产品型号、编号、独特图形、独特版式、独特口号、联系方式、地址、注册码、序列号等有助于唯一识别对象的信息，必须在“唯一性、特征性、标识性信息”中单独提炼列出。
- 下列采集上下文是辅助信息，可用于增强理解，但不能覆盖图片可见事实。
- 采集时间：{captured_at}
- 采集经纬度：{capture_location}
- 采集地址：{capture_address}
- 地点 place_id：{capture_place_id}
- 地理精度类型：{capture_location_type}
- 地理结果类型：{capture_result_types}
- 来源客户端：{source_client}
- 命名空间：{namespace_id}
{user_note_section}
